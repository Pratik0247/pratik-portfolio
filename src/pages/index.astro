---
import { getCollection } from "astro:content";
import FloatingNav from "../components/FloatingNav.astro";
import ResumeMenu from "../components/ResumeMenu.astro";
import BaseLayout from "../layouts/BaseLayout.astro";

const experience = await getCollection("experience");
const projects = await getCollection("projects");
const leadership = await getCollection("leadership"); // we can add this collection

// Sort experience by start date in descending order (most recent first)
const sortedExperience = experience.sort((a, b) => {
  return b.data.start.localeCompare(a.data.start);
});
---

<BaseLayout title="Home">
  <section
    id="hero"
    class="h-screen flex flex-col justify-center items-center text-center px-6"
  >
    <!-- Name -->
    <h1
      class="text-6xl md:text-7xl font-extrabold text-[var(--ghost-white)] mb-4 opacity-0 animate-fade-in"
      style="animation-delay: 0.1s"
    >
      Pratik Bhirud
    </h1>

    <!-- Role -->
    <h2
      class="text-3xl md:text-4xl font-semibold text-[var(--light-blue)] mb-8 opacity-0 animate-fade-in"
      style="animation-delay: 0.5s"
    >
      Software Engineer
    </h2>

    <!-- Tagline -->
    <p
      class="mt-2 text-lg md:text-xl text-[var(--light-blue)]/90 max-w-2xl leading-relaxed opacity-0 animate-fade-in"
      style="animation-delay: 1s"
    >
      I design and build data systems, cloud infrastructure, and ML-powered
      applications. <br />
      Currently pursuing my MS in Computer Science at CU Boulder.
    </p>

    <!-- Buttons -->
    <div
      class="mt-10 flex flex-wrap justify-center gap-4 opacity-0 animate-fade-in"
      style="animation-delay: 1.5s"
    >
      <ResumeMenu label="Download Resume" id="hero" />
      <a href="#experience" class="btn liquid-glass-btn">See Experience</a>
      <a href="#projects" class="btn liquid-glass-btn">See Projects</a>
      <a href="#contact" class="btn liquid-glass-btn">Contact Me</a>
    </div>
  </section>

  <!-- Liquid Glass Scroll Arrow -->
  <!-- Scroll Chevron -->
  <div
    class="absolute bottom-6 right-6 md:bottom-8 md:left-1/2 md:right-auto
         transform md:-translate-x-1/2
         md:opacity-0 animate-fade-in"
    style="animation-delay: 2s;"
  >
    <a
      href="#experience"
      class="w-12 h-12 md:w-14 md:h-14 rounded-full flex items-center justify-center
           border border-white/20 bg-white/10 backdrop-blur-md
           text-[var(--light-blue)]
           hover:text-[var(--ghost-white)] hover:border-[var(--light-blue)]
           hover:shadow-[0_0_20px_var(--light-blue)]
           transition-all duration-300 ease-out"
    >
      <svg
        xmlns="http://www.w3.org/2000/svg"
        class="w-6 h-6"
        fill="none"
        viewBox="0 0 24 24"
        stroke="currentColor"
        stroke-width="2"
      >
        <!-- Chevron Down (no stick) -->
        <path stroke-linecap="round" stroke-linejoin="round" d="M6 9l6 6 6-6"
        ></path>
      </svg>
    </a>
  </div>

  <!-- Experience -->
  <section id="experience" class="py-20 px-6 section-animate">
    <h2 class="text-3xl font-bold text-[var(--ghost-white)] mb-10">
      Experience
    </h2>
    <div class="space-y-6 md:space-y-8">
      {
        sortedExperience.map((exp, index) => (
          <div class="card card-animate">
            <div class="flex flex-col md:flex-row md:items-start md:justify-between mb-3 gap-2">
              <h3 class="text-lg md:text-xl font-bold text-[var(--ghost-white)] leading-tight">
                {exp.data.company} — {exp.data.role}
              </h3>
              <span class="text-xs md:text-sm text-[var(--light-blue)] bg-[var(--light-blue)]/10 px-3 py-1 rounded-full self-start md:self-auto">
                {exp.data.period}
              </span>
            </div>
            <ul class="text-sm md:text-base text-[var(--light-blue)] space-y-2">
              {exp.data.highlights?.map((h) => (
                <li class="flex items-start gap-3">
                  <div class="w-1.5 h-1.5 rounded-full bg-[var(--light-blue)] mt-2 flex-shrink-0" />
                  <span class="leading-relaxed">{h}</span>
                </li>
              ))}
            </ul>
          </div>
        ))
      }
    </div>
  </section>

  <!-- Projects -->
  <section id="projects" class="py-20 px-6 section-animate">
    <h2 class="text-3xl font-bold text-[var(--ghost-white)] mb-10">Projects</h2>
    <div class="space-y-8">
      {
        projects.map((p) => (
          <div class="card card-animate">
            <h3 class="text-xl font-bold text-[var(--ghost-white)]">
              {p.data.title}
            </h3>
            <p class="text-sm text-[var(--light-blue)]">{p.data.summary}</p>
            <a
              href={`/projects/${p.slug}`}
              class="text-sm text-[var(--light-blue)] hover:text-[var(--ghost-white)] hover:underline mt-2 inline-block"
            >
              Read more →
            </a>
          </div>
        ))
      }
    </div>
  </section>

  <!-- Leadership -->
  <section id="leadership" class="py-20 px-6 section-animate">
    <h2 class="text-3xl font-bold text-[var(--ghost-white)] mb-10">
      Leadership & Involvement
    </h2>
    <div class="space-y-8">
      {
        leadership.map((l) => (
          <div class="card card-animate">
            <h3 class="text-xl font-bold text-[var(--ghost-white)]">
              {l.data.company} — {l.data.role}
            </h3>
            <p class="text-sm text-[var(--light-blue)]">{l.data.period}</p>
            <ul class="mt-3 list-disc list-inside text-[var(--light-blue)] space-y-1">
              {l.data.highlights?.map((h) => (
                <li>{h}</li>
              ))}
            </ul>
          </div>
        ))
      }
    </div>
  </section>

  <!-- Contact -->
  <section id="contact" class="py-20 px-6 text-center section-animate">
    <h2 class="text-3xl font-bold text-[var(--ghost-white)] mb-10">
      Get in Touch
    </h2>

    <div
      class="card inline-block text-center max-w-lg mx-auto space-y-6 card-animate"
    >
      <p class="text-[var(--light-blue)]">
        Lets connect! — You can reach me on any platform below:
      </p>

      <div class="flex justify-center gap-4 flex-wrap">
        <a
          href="mailto:pratikbhirud04@gmail.com"
          class="px-4 py-2 rounded-xl border border-white/10
               bg-white/5 backdrop-blur-md text-[var(--light-blue)]
               hover:border-[var(--light-blue)] hover:text-[var(--ghost-white)]
               hover:shadow-[0_0_10px_var(--light-blue)] liquid-glass-btn"
        >
          📧 Email
        </a>

        <a
          href="https://linkedin.com/in/pratik-bhirud"
          target="_blank"
          class="px-4 py-2 rounded-xl border border-white/10
               bg-white/5 backdrop-blur-md text-[var(--light-blue)]
               hover:border-[var(--light-blue)] hover:text-[var(--ghost-white)]
               hover:shadow-[0_0_10px_var(--light-blue)] liquid-glass-btn"
        >
          🔗 LinkedIn
        </a>

        <a
          href="https://github.com/Pratik0247"
          target="_blank"
          class="px-4 py-2 rounded-xl border border-white/10
               bg-white/5 backdrop-blur-md text-[var(--light-blue)]
               hover:border-[var(--light-blue)] hover:text-[var(--ghost-white)]
               hover:shadow-[0_0_10px_var(--light-blue)] liquid-glass-btn"
        >
          🖥 GitHub
        </a>
      </div>
    </div>
  </section>

  <FloatingNav />
</BaseLayout>

<script>
  // Attach ripple effect to all buttons with .liquid-glass-btn
  document.querySelectorAll(".liquid-glass-btn").forEach((btn) => {
    btn.addEventListener("click", (e) => {
      const rect = btn.getBoundingClientRect();
      const ripple = btn.querySelector("::before"); // can't access pseudo directly

      // Instead: set CSS vars for click pos
      const x = e.clientX - rect.left;
      const y = e.clientY - rect.top;

      btn.style.setProperty("--ripple-x", `${x}px`);
      btn.style.setProperty("--ripple-y", `${y}px`);

      btn.classList.remove("ripple");
      void btn.offsetWidth;
      btn.classList.add("ripple");
    });
  });
</script>

<script>
  document.querySelectorAll(".card").forEach((card) => {
    card.addEventListener("mousemove", (e) => {
      const rect = card.getBoundingClientRect();
      const x = e.clientX - rect.left;
      const y = e.clientY - rect.top;
      card.style.setProperty("--cursor-x", `${x}px`);
      card.style.setProperty("--cursor-y", `${y}px`);
    });
  });
</script>

<script>
  const sections = document.querySelectorAll(".section-animate");

  const observer = new IntersectionObserver(
    (entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          entry.target.classList.add("section-slide-up");
          observer.unobserve(entry.target); // Only animate once
        }
      });
    },
    { threshold: 0.2 }, // Trigger when 20% of section is visible
  );

  sections.forEach((section) => observer.observe(section));
</script>

<script>
  const sections = document.querySelectorAll(".section-animate");

  const sectionObserver = new IntersectionObserver(
    (entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          entry.target.classList.add("section-slide-up");

          // Animate cards inside
          const cards = entry.target.querySelectorAll(".card-animate");
          cards.forEach((card, i) => {
            setTimeout(() => {
              card.classList.add("card-slide-left");
            }, i * 150); // stagger by 150ms
          });

          sectionObserver.unobserve(entry.target); // only once
        }
      });
    },
    { threshold: 0.2 },
  );

  sections.forEach((section) => sectionObserver.observe(section));
</script>

<script>
  const scrollTopBtn = document.getElementById("scrollTopBtn");

  window.addEventListener("scroll", () => {
    if (window.scrollY > window.innerHeight * 0.7) {
      scrollTopBtn.classList.remove("hidden");
    } else {
      scrollTopBtn.classList.add("hidden");
    }
  });
</script>
